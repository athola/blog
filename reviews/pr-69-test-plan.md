# Test plan — PR #69

This complements the existing `## Test plan` block in the PR description (T32 visual regression + T33 Lighthouse). The items below are the **gates that must pass for merge**, not the visual-quality checks (those should still happen, but they don't gate the merge).

## Pre-merge verification (CI-gated)

- [ ] **B1 fix verified locally**: `cargo clippy --all-targets --workspace -- -D warnings` exits 0 (currently fails on `tests/server_integration_tests.rs:1268` and `:1296`).
- [ ] **B2 fix verified locally**: `docker build .` (or `make docker` if the Makefile has a target) succeeds end-to-end with the wasm-bindgen pin aligned. Spot-check that `target/site/pkg/<wasm-binary>` is generated and non-empty.
- [ ] **Full integration suite green**: `cargo test --workspace --all-targets` exits 0 once the clippy fix lands.
- [ ] **CI status**: `gh pr checks 69` shows all required checks at SUCCESS or SKIPPED — no FAILURE, no IN_PROGRESS at the time of merge.
- [ ] **Test-DB cleanup invariant holds**: after `cargo test --workspace --all-targets`, `find /tmp -maxdepth 2 -name 'rustblog_test_*' -type d | wc -l` returns `0`.

## Functional smoke (post-fix, run locally)

- [ ] `make watch` boots without panic; `curl -s http://127.0.0.1:3007/health` returns `{"status":"healthy",...}`.
- [ ] **New routes return 200 in dev**: `for r in /archive /about /colophon /notes; do curl -sI "http://127.0.0.1:3007$r" | head -1; done` — all `200 OK`.
- [ ] **Renames + redirects**:
  - `curl -sI http://127.0.0.1:3007/activity` → `301` to `/notes`.
  - `curl -sI http://127.0.0.1:3007/random` → `302` to a `/post/<slug>` URL (or `404`/`503` if no posts seeded — all acceptable).
- [ ] **Feed envelopes**:
  - `curl -s http://127.0.0.1:3007/feed/feed.xml | head -2` shows `<feed xmlns="http://www.w3.org/2005/Atom">`.
  - `curl -s http://127.0.0.1:3007/feed/rss.xml | head -2` shows `<rss` or `<?xml`.
- [ ] **Raw markdown alternate**: `curl -sI http://127.0.0.1:3007/post/<seeded-slug>/raw.md` returns `Content-Type: text/markdown; charset=utf-8` and a non-empty body.

## Visual / quality (manual — already in PR description)

- [ ] T32 — Visual regression in light + dark for `/`, `/post/<slug>`, `/archive`, `/notes`, `/references`, `/about`, `/contact`, `/colophon` (before/after vs `master`).
- [ ] T33 — Lighthouse on `/` and `/post/<slug>`: Perf ≥ 90, A11y ≥ 95, BP ≥ 90, SEO ≥ 95.
- [ ] W3C Atom Feed validator against `/feed/feed.xml` post-deploy.
- [ ] Eyeball-check each new route in light + dark on a real device.

## Post-merge / pre-deploy

- [ ] Sitemap updated for `/archive`, `/notes`, `/about`, `/colophon` (N2 from review). If landed in a follow-up PR, file the issue immediately and link it here.
- [ ] `.attune/mission-state.json` policy decided (N1 — gitignore vs. relocate vs. keep).

## What's NOT in this test plan and why

- **Pre-existing tests on `master`**: assumed already covered. This plan is delta-only.
- **Browser-based interaction tests** (theme toggle, focus traps, etc.): the toggle UI is explicitly deferred per spec §8 — there's nothing to test until the next branch.
- **Newsletter integration**: spec deferred.
- **JSON Feed**: spec deferred.

---

*Generated by `/sanctum:pr-review`. The two checked-in CI failures (B1 + B2) are mechanical fixes and are the only hard blockers between this PR and an approve. See the review comment above for full context.*
