# Example environment variables for the blog project
# Copy this to .env and fill in your actual values

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Google Gemini AI API Key
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# SurrealDB Configuration (Development)
SURREAL_PROTOCOL=http
SURREAL_HOST=127.0.0.1:8000
SURREAL_ROOT_USER=root
SURREAL_ROOT_PASS=root
SURREAL_NS=rustblog
SURREAL_DB=rustblog

# Leptos Configuration (Development)
LEPTOS_SITE_ADDR=127.0.0.1:3007
LEPTOS_SITE_ROOT=site
LEPTOS_HASH_FILES=false
LEPTOS_RELOAD_PORT=3001

# Logging
RUST_LOG=debug

# Environment
RUST_ENV=development

# SMTP Configuration (if needed for contact form)
# SMTP_HOST=smtp.example.com
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_smtp_password

# =============================================================================
# PRODUCTION CONFIGURATION (.env.production)
# =============================================================================
# Use these variables for production deployment on DigitalOcean
# Set these as encrypted environment variables in DigitalOcean App Platform

# Environment
# RUST_ENV=production

# Leptos Configuration (Production)
# LEPTOS_SITE_ADDR=0.0.0.0:8080
# LEPTOS_SITE_ROOT=site
# LEPTOS_HASH_FILES=true
# LEPTOS_RELOAD_PORT=3001

# Logging (Production)
# RUST_LOG=info

# SurrealDB Configuration (Production)
# IMPORTANT: Set these as encrypted secrets in DigitalOcean App Platform
#
# Connection settings (required):
# SURREAL_ADDRESS=http://YOUR_DROPLET_IP:8000
# SURREAL_NS=production
# SURREAL_DB=alexthola_blog
#
# =============================================================================
# AUTHENTICATION OPTIONS (choose ONE method)
# =============================================================================
#
# OPTION 1: Root Authentication (RECOMMENDED for simple setups)
# Use the same credentials you started SurrealDB with.
# This is the simplest option if you're using root credentials.
# SURREAL_ROOT_USER=root
# SURREAL_ROOT_PASS=YOUR_ROOT_PASSWORD
#
# OPTION 2: Database-Level Authentication (for fine-grained access)
# Requires creating a user in SurrealDB first:
#   DEFINE USER appuser ON DATABASE PASSWORD 'password' ROLES OWNER;
# Then set:
# SURREAL_USERNAME=appuser
# SURREAL_PASSWORD=YOUR_DB_USER_PASSWORD
#
# OPTION 3: Namespace-Level Authentication (for multi-database access)
# Requires creating a namespace user in SurrealDB first:
#   DEFINE USER nsuser ON NAMESPACE PASSWORD 'password' ROLES OWNER;
# Then set:
# SURREAL_NAMESPACE_USER=nsuser
# SURREAL_NAMESPACE_PASS=YOUR_NS_USER_PASSWORD
#
# COMMON MISTAKE: Do NOT use SURREAL_USERNAME with root credentials!
# Root is a root-level user, not a database-level user.

# Security Best Practices:
# 1. NEVER commit .env or .env.production to git
# 2. Use strong passwords (min 32 characters, generated with: openssl rand -base64 32)
# 3. Rotate credentials every 90 days
# 4. Use encrypted environment variables in DigitalOcean
# 5. Restrict database access to your app's VPC only