name: Security - Secret Scanning

on:
  push:
    branches: [ "master" ]
    paths-ignore:
      - 'target/**'
      - 'node_modules/**'
  pull_request:
    branches: [ "master" ]
    paths-ignore:
      - 'target/**'
      - 'node_modules/**'
  schedule:
    - cron: '0 2 * * 0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  secrets-scan:
    name: Scan for secrets
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Run secret scanning
      run: |
        chmod +x ./run_secret_scan.sh
        ./run_secret_scan.sh

    - name: Analyze scanning results
      id: analyze
      run: |
        GITLEAKS_SECRETS=0
        SEMGREP_HIGH=0
        if [ -f secret_scanning_results/gitleaks-report.json ]; then
          # Check if the file is not empty and contains valid JSON
          if [ -s secret_scanning_results/gitleaks-report.json ] && jq empty secret_scanning_results/gitleaks-report.json 2>/dev/null; then
            # Only count non-empty results array (jq type returns quoted string like "array")
            if [ "$(jq 'type' secret_scanning_results/gitleaks-report.json 2>/dev/null)" = "\"array\"" ]; then
              if [ "$(jq 'length' secret_scanning_results/gitleaks-report.json 2>/dev/null)" -gt 0 ]; then
                GITLEAKS_SECRETS=$(jq 'length' secret_scanning_results/gitleaks-report.json 2>/dev/null || echo "0")
              fi
            fi
          fi
        fi
        if [ -f secret_scanning_results/semgrep-report.json ]; then
          # Check if the file is not empty and contains valid JSON
          if [ -s secret_scanning_results/semgrep-report.json ] && jq empty secret_scanning_results/semgrep-report.json 2>/dev/null; then
            SEMGREP_HIGH=$(jq '[.results[]? | select(.extra.severity == "ERROR")] | length' secret_scanning_results/semgrep-report.json 2>/dev/null || echo "0")
          fi
        fi
        echo "gitleaks_secrets=$GITLEAKS_SECRETS" >> $GITHUB_OUTPUT
        echo "semgrep_high=$SEMGREP_HIGH" >> $GITHUB_OUTPUT

    - name: Fail on critical secrets
      if: steps.analyze.outputs.gitleaks_secrets != '0' || steps.analyze.outputs.semgrep_high != '0'
      run: |
        echo "Critical security issues detected!"
        exit 1

    - name: Upload secret scanning results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: secret-scanning-results-${{ github.run_number }}
        path: secret_scanning_results/
        retention-days: 90
